---
import { Icon } from 'astro-icon/components';
import { profile, type SocialLink } from '../data/profile';

interface Props {
	size?: number;
	socials?: SocialLink[];
}

const { size = 20, socials = profile.socials } = Astro.props;

const iconMap = {
	mail: 'tabler:mail',
	linkedin: 'tabler:brand-linkedin',
	github: 'tabler:brand-github',
	facebook: 'tabler:brand-facebook',
	instagram: 'tabler:brand-instagram',
	x: 'tabler:brand-x',
};
---

<div class="flex items-center gap-4">
	{socials.map((social) => (
		<a
			href={social.url}
			aria-label={social.label}
			target="_blank"
			rel="noopener noreferrer"
			class="text-slate-600 hover:text-slate-900 dark:text-slate-400 dark:hover:text-white transition-colors"
		>
			<Icon name={iconMap[social.icon]} size={size} />
		</a>
	))}
</div>
